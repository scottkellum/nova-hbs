<?xml version="1.0" encoding="UTF-8"?>
<completions>
    <provider>
        <selector>hbs</selector>
        <expression>\b[a-zA-Z0-9-_]*</expression>
        <set>hbs.entities</set>
    </provider>
    
    <!-- Entities -->
    <set name="hbs.entities">
		<completion string="{{" />
		<completion string="}}" />
    </set>
</completions>
